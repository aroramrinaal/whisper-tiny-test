{
  "name": "whisper-tiny-test",
  "scripts": {
    "dev": "wrangler dev --port 3001",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.28"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
